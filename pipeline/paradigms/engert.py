"""Engert GCaMP paradigm — processing stage is a no-op (Suite2p is pre-computed)."""


def run_processing(cfg):
    print(f"[engert] {cfg.name}: no processing stage — Suite2p outputs are pre-computed")
